package arrays_and_string_manipulation

func findDuplicateLogs(logTimes []int) {
	// Find logs that have duplicate time stamps

	// add each entry to a map of timestamp to entry. If we find a duplicate value, we've found the duplicate log

	// how do we handle
}
